    <div class="navbar navbar-inverse navbar-static-top">
      <div class="row">
        <div class="col-sm-2">
        <%= link_to "Project Tracker", root_path, class: "navbar-brand", id: "logo", style: 'color:#ffffff;' %></a>
        </div>
        <div class="col-sm-1 navbar-collapse collapse navbar-responsive-collapse">
          <% if current_user.present? %>
          <ul class="nav navbar-nav">
            <li class="dropdown active">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">Project<span class="caret"></span></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "All Projects", projects_path %></li>
                  <% if current_user.try(:admin) %>
                    <li><%= link_to "New Project", new_project_path %></li>
                  <% end %>
                </ul>
            </li>
          </ul>
          <% end %>
        </div>

     
          <div align="right" class="col-sm-offset-5 col-sm-3"> 
              <% if current_user.present? %> 
              <h4 style="color: white; padding-top: 5px;">Welcome, <%= current_user.full_name %></h4>
              <% end %>
          </div>

          <div class="col-sm-1 navbar-collapse collapse navbar-responsive-collapse">
          <% if current_user.present? %>
          <ul class="nav navbar-nav">
            <li class="dropdown active">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#"><i align="right" class="glyphicon glyphicon-tasks"></i></a>
              
              <ul class="dropdown-menu">
                <%if current_user.try(:admin) %>
                <li><%= link_to "Tasks", tasks_path %></li>
                <li><%= link_to "Products", products_path %></li>
                <% end %>
                <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>

              </ul>
             </li>
            </ul>
            <% end %>
          </div>
        </div>  
      </div>